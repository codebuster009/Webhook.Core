# Copy to .env in backend/ and repo root as needed
DATABASE_URL=postgresql://webhook:webhook@localhost:5432/webhook?schema=public
PORT=3000
NODE_ENV=development
LOG_LEVEL=info
WORKER_CONCURRENCY=4
LEASE_SWEEP_MS=30000
IN_FLIGHT_LEASE_SECONDS=60

# Frontend (Vite)
VITE_API_BASE_URL=http://localhost:3000
